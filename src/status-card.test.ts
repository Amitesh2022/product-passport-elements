// @vitest-environment jsdom
import{beforeEach,describe,expect,it}from'vitest';import'./product-card';import type{ProductCard}from'./product-card';describe('product-card',()=>{let element:ProductCard;beforeEach(async()=>{element=document.createElement('product-card');element.name='Circular office chair';element.maker='Brussels Works';element.verified=true;document.body.append(element);await element.updateComplete});it('renders passport evidence',()=>{expect(element.shadowRoot?.textContent).toContain('Circular office chair');expect(element.shadowRoot?.textContent).toContain('Evidence verified')});it('emits a composed selection event',()=>{let selected=false;element.addEventListener('select-product',()=>selected=true);element.shadowRoot?.querySelector('button')?.click();expect(selected).toBe(true)})})
